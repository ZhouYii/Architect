id: id_1774767321817_24
description: >
  The autosave engine writes to project storage via Rust IPC (write_files
  command).
status: accepted
timestamp: 2026-03-29T06:55:21.817Z
changes:
  - kind: add_arrow
    target_canvas: root
    reason: >
      The autosave engine writes to project storage via Rust IPC (write_files
      command).
    proposal:
      id: autosave-writes
      from: autosave
      to: project-storage
      label: writes changed files
      type: writes
      weight: bold
