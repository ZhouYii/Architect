id: id_1774767321817_23
description: >
  The canvas doesn't persist directly — it goes through the autosave engine.
  Repoint the arrow to reflect the actual data path.
status: accepted
timestamp: 2026-03-29T06:55:21.817Z
changes:
  - kind: modify_arrow
    target_canvas: root
    target_id: canvas-persists
    reason: >
      The canvas doesn't persist directly — it goes through the autosave engine.
      Repoint the arrow to reflect the actual data path.
    proposal:
      from: visual-canvas
      to: autosave
      label: triggers flush on change
      type: calls
      weight: bold
